"""Pre-defined LangGraph workflows.

Hand-written StateGraph DAGs whose node order is fixed at compile time.
Distinct from `app.agents`, which holds LLM-driven ReAct agents whose
node order is decided per-task by the model.
"""
