# Dependencies
node_modules/
__pycache__/
*.pyc
*.pyo

# Environment
.env
backend/.env

# Build artifacts
.next/
out/
dist/
build/
*.egg-info/

# MLflow
backend/mlruns/
mlruns/

# Temp & Data
tmp_cure/
backend/.venv/
.venv/

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Docker
*.log

# Rerun
/logs
*.rrd
# Local clone of private cure repo (install via pip per CLAUDE.md)
backend/cure/

# Claude Code agent state
.claude/

# Camera bridge: self-signed certs (regenerated per install) + binary distributions
backend/cam_bridge/cert.pem
backend/cam_bridge/key.pem
# MediaMTX TLS certs — mkcert-issued; private key MUST NOT be committed.
# Bootstrap: regenerate via `mkcert localhost 192.168.1.47` (run `mkcert -install` once).
backend/mediamtx/cert.pem
backend/mediamtx/key.pem
backend/mediamtx/mediamtx
backend/mediamtx/mediamtx_v*.tar.gz
backend/mediamtx/*.bak

# Brainstorming visual-companion session artifacts (HTML mockups, state, logs)
.superpowers/

# Verification-script screenshot output (regenerated by scripts/verify_*.py)
scripts/_out/
