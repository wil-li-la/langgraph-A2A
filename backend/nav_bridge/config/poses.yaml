# Named navigation poses — used by Phase 2 navigate_to("medicine") etc.
# Phase 1 uses raw (x, y, theta) only; this file is consumed when the
# medication_delivery workflow switches to the new client.
#
# All poses are in the map frame (frame_id: map), theta in radians.
# Fill in by manually driving the robot to each location and recording
# the pose from /api/nav/pose.

medicine: [0.0, 0.0, 0.0]   # TBD — pharmacy pickup pose
patient:  [0.0, 0.0, 0.0]   # TBD — patient bedside
origin:   [0.0, 0.0, 0.0]   # TBD — charging dock
